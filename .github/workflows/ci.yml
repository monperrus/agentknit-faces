name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.x"

      - name: Install the faces (with the TUI extra) and test tooling
        run: |
          pip install --upgrade pip
          pip install ".[tui]" pytest "ruff>=0.15.12" build uv

      - name: Lint with ruff
        run: |
          # Rule selection is pinned in pyproject.toml ([tool.ruff.lint] select),
          # so any recent ruff works.
          ruff check agent-*.py tests/

      - name: Run tests
        run: python -m pytest -v

      - name: Build wheel
        run: |
          ./build-modules.sh
          python -m build --wheel

      - name: Smoke-test entry points from the wheel
        run: |
          uvx --from dist/agentknit_faces-*.whl python -c \
            "import agent_glm_5_3, agent_glm_4_5_air, agent_deepseek_v4_flash, agent_kimi_k3; print('faces import OK')"
